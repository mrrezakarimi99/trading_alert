# =============================================================================
# DOCKER COMPOSE - MULTI-SERVICE DEPLOYMENT
# =============================================================================
# Usage:
# - Start all:     docker-compose up -d
# - Data fetch:    docker-compose up data-fetcher
# - Training:      docker-compose up trainer
# - Trading only:  docker-compose up crypto-trading
# - Stop:          docker-compose down
# - Logs:          docker-compose logs -f [service-name]
# - Status:        docker-compose ps
# =============================================================================



services:
  # =============================================================================
  # DATA FETCHER SERVICE - Fetches historical data
  # =============================================================================
  data-fetcher:
    build: .
    container_name: crypto-data-fetcher
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=UTC
      - SERVICE_MODE=fetch
      # TensorFlow CPU-only configuration
      - TF_CPP_MIN_LOG_LEVEL=2
      - CUDA_VISIBLE_DEVICES=0
    volumes:
      - data_volume:/app/data
        - ./src:/app/src
      - ./.env:/app/.env:ro
    user: "1000:1000"
    entrypoint: ["/app/entrypoint.sh"]
    command: ["python", "main.py", "--fetch-data", "--days", "365"]
    restart: "no"  # Run once and exit
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # =============================================================================
  # MODEL TRAINER SERVICE - Trains ML models
  # =============================================================================
  trainer:
    build: .
    container_name: crypto-trainer
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=UTC
      - SERVICE_MODE=train
      # TensorFlow CPU-only configuration
      - TF_CPP_MIN_LOG_LEVEL=2
      - CUDA_VISIBLE_DEVICES=0
      - TF_FORCE_GPU_ALLOW_GROWTH=false
      # Matplotlib configuration
      - MPLCONFIGDIR=/tmp/matplotlib
      - MPLBACKEND=Agg
    volumes:
      - data_volume:/app/data
      - models_volume:/app/src/ml_models
      - ./.env:/app/.env:ro
    tmpfs:
      - /tmp:noexec,nosuid,size=200m
    user: "1000:1000"
    entrypoint: ["/app/entrypoint.sh"]
    command: ["python", "main.py", "--train", "--days", "365"]
    restart: "no"  # Run once and exit
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # =============================================================================
  # TRADING SERVICE - Main trading system
  # =============================================================================
  crypto-trading:
    build: .
    container_name: crypto-trading-system
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=UTC
      - SERVICE_MODE=trade
      # TensorFlow CPU-only configuration
      - TF_CPP_MIN_LOG_LEVEL=2
      - CUDA_VISIBLE_DEVICES=0
      - TF_FORCE_GPU_ALLOW_GROWTH=false
      # Matplotlib configuration
      - MPLCONFIGDIR=/tmp/matplotlib
      - MPLBACKEND=Agg
    volumes:
      - data_volume:/app/data
      - models_volume:/app/src/ml_models
      - ./.env:/app/.env:ro
    user: "1000:1000"
    entrypoint: ["/app/entrypoint.sh"]
    command: ["python", "main.py"]  # Default live trading
    # Resource limits
    mem_limit: 1g
    cpus: 0.5
    # Health check
    healthcheck:
      test: ["CMD", "python", "-c", "import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s  # Give more time for model loading and dependency check
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # =============================================================================
  # INIT SERVICE - Runs complete initialization pipeline
  # =============================================================================
  init-pipeline:
    build: .
    container_name: crypto-init-pipeline
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=UTC
      - SERVICE_MODE=init
      # TensorFlow CPU-only configuration
      - TF_CPP_MIN_LOG_LEVEL=2
      - CUDA_VISIBLE_DEVICES=0
    volumes:
      - data_volume:/app/data
      - models_volume:/app/src/ml_models
      - ./.env:/app/.env:ro
    user: "1000:1000"
    entrypoint: ["/app/entrypoint.sh"]
    command: ["/app/service-manager.sh", "health-check"]
    restart: "no"  # Run once and exit
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # =============================================================================
  # WATCHTOWER - Auto-updates containers
  # =============================================================================
  watchtower:
    image: containrrr/watchtower
    container_name: crypto-trading-watchtower
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_POLL_INTERVAL=3600  # Check for updates every hour
    command: crypto-trading-system

volumes:
  data_volume:
    driver: local
  models_volume:
    driver: local

networks:
  default:
    name: crypto-trading-network